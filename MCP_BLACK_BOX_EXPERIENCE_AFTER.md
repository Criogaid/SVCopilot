# SV Copilot MCP 黑盒专项测试报告与优化建议

## 1. 测试约束

本轮测试严格把 `sv-copilot` 当作独立安装的 MCP 使用。

- 未读取仓库代码。
- 未读取项目内部文档或本地 API 清单。
- 方法签名不明确时，依据 MCP 自身的错误反馈继续测试。
- 只使用 MCP 返回值和 Synthesizer V 当前工程状态作为证据。
- 所有临时修改均尝试恢复原状。

## 2. 结论摘要

本轮五类高价值能力均能通过 MCP 触达：

1. 精细音素覆盖：通过。
2. 详细计算属性与计算音高读取：通过，但返回结构需要改进。
3. Pitch Control Point 与 Curve：通过。
4. Retake 生命周期：通过，但索引规则和无效操作反馈存在问题。
5. 选区驱动编辑：通过，但空集合和布尔返回值不可靠。

这说明 `sv-copilot` 已经具备成为调声助手底层执行器的关键能力。当前主要瓶颈不再是“能否调用 SynthV”，而是批量性、结果结构、状态判定、索引一致性和高层工作流。

## 3. 测试结果

### 3.1 精细音素覆盖

测试对象：主唱轨第一个歌词为「さ」的音符。

原始状态：

- 显式音素覆盖：空字符串。
- 计算音素：`s a`。

临时操作：

- 将显式音素覆盖设为 `s e`。

观察结果：

- 音符读回覆盖值为 `s e`。
- 计算音素同步变为 `s e`。
- 歌词本身没有改变。

恢复结果：

- 将覆盖恢复为空字符串。
- 计算音素恢复为 `s a`。

结论：通过。

这项能力可直接用于假名发音修正、跨语言发音、辅音替换和特殊唱法。相比只改歌词，它已经具备真正的发音编辑价值。

### 3.2 详细计算属性

对主唱音符组读取到 69 条详细计算结果，与 69 个音符一一对应。

实际观察到的内容包括：

- 计算后的音素符号。
- 音素语言。
- 辅音活动度。
- 辅音位置。
- Accent。
- Rap Tone。
- Rap Intonation。

例如首音「さ」返回了 `s` 与 `a`，其中辅音 `s` 还带有活动度和位置。

结论：通过。

这项能力非常适合用于：

- 写后验证发音是否符合预期。
- 定位辅音过早或过晚。
- 检查跨语言音素。
- 为自动发音修正提供反馈数据。
- 分析说唱语调与重音。

### 3.3 计算音高

首次仅传音符组时，MCP 返回了明确的参数错误，并给出所需签名：

`(groupReference, blickStart, blickInterval, numFrames)`

随后使用 1/16 拍采样间隔请求 128 帧：

- 实际有音高数据的帧数：106。
- 第一个有声帧索引：8。
- 最后一个有声帧索引：122。
- 该区间计算音高范围约为 MIDI `52.83–70.12`。

结论：功能通过，返回结构部分不通过。

问题：

- 有休止的结果没有返回标准数组。
- 返回值是以数字字符串为键的稀疏对象。
- 调用方必须自行排序键并恢复帧索引。
- 空白帧是“缺少键”，不是明确的 `null`。

建议返回以下任一稳定结构：

```json
{
  "blickStart": 0,
  "blickInterval": 44100000,
  "values": [null, null, 62.56, 61.84]
}
```

或：

```json
{
  "frames": [
    { "index": 8, "pitch": 62.56 },
    { "index": 9, "pitch": 61.84 }
  ]
}
```

### 3.4 Pitch Control Point

测试前控制对象数量：0。

操作：

1. 创建一个 Pitch Control Point。
2. 设置位置为 `529200000` blick。
3. 设置音高为 `61.5`。
4. 添加到主唱音符组。
5. 从音符组重新取得控制点。
6. 将音高改为 `62.25`。
7. 删除控制点。

观察结果：

- 添加索引为 1。
- 数量从 0 变为 1。
- 位置与音高均可读回。
- 修改后的 `62.25` 可读回。
- 删除后数量恢复为 0。

结论：通过。

### 3.5 Pitch Control Curve

操作：

1. 创建一个 Pitch Control Curve。
2. 设置锚点位置和基础音高。
3. 写入三个相对控制点。
4. 添加到音符组。
5. 读取点数组与曲线值。
6. 删除并确认数量恢复。

观察结果：

- 空曲线的 `getPoints` 返回 `{}`，不是 `[]`。
- 写入后点数组能正常读回。
- 曲线能添加到音符组并删除。
- 控制点数组的位置是相对坐标。
- `getValueAt` 的查询位置使用绝对工程坐标。
- 相对中点查询得到基础音高 `64`。
- 绝对中点查询得到插值结果 `64.5`。

结论：生命周期通过，坐标语义需要在 MCP 层显式说明或统一。

### 3.6 Retake

测试对象：主唱首音。

原始 Take 数量：1。

无参调用时，MCP 返回所需签名：

`generateTake(newDuration, newPitch, newTimbre)`

使用三个布尔值生成临时 Take 后：

- `generateTake` 返回索引 1。
- Take 数量从 1 增至 2。
- 可以切换当前 Take。

首次清理错误：

- 按其他 Lua 集合习惯尝试删除索引 2。
- 调用没有报错。
- Take 数量仍为 2。

随后按生成返回值处理：

- 将活动 Take 恢复为索引 0。
- 删除索引 1。
- Take 数量恢复为 1。

结论：通过，但 Retake 明确使用 0-based 索引，并且越界删除是静默无效操作。

这与音轨、音符和音符组常见的 1-based 使用方式形成明显冲突。

### 3.7 选区驱动编辑

当前编辑器状态：

- 当前轨道可读取。
- 当前音符组可读取。
- 当前组为主组。
- 当前选区对象可读取。
- 无选中音符时返回 `{}`。
- 无选中音符组时返回 `{}`。

临时测试：

1. 通过选区对象选择怒声轨第一个音符。
2. 再从选区读取选中音符句柄。
3. 使用该句柄将 Detune 从 0 改为 5 cents。
4. 读回 5。
5. 恢复为 0。
6. 取消临时选择。

结果：

- 选区成功返回 1 个音符。
- Detune 的写入和恢复均成功。
- 最终选区恢复为空。
- `unselectNote` 返回 `false`，但实际已经取消选择。

结论：选区驱动编辑通过，返回值语义不可靠。

## 4. 测试后的工程状态

本轮测试结束时：

- 首音显式音素覆盖已恢复为空。
- 首音计算音素已恢复为 `s a`。
- 主唱音符组 Pitch Control 数量已恢复为 0。
- Retake 数量已恢复为 1。
- 临时 Detune 已恢复为 0。
- 临时音符选区已取消。
- 原有主唱轨与怒声叠唱轨未被删除或重写。

## 5. 作为调音和填词助手的能力评价

### 5.1 已具备的关键底层能力

#### 填词与发音

- 改歌词。
- 指定语言。
- 写入显式音素。
- 读取计算音素。
- 读取音素活动度与位置。

#### 调音

- 改音高、时值、Detune。
- 写入 Pitch Delta 与其他参数自动化。
- 创建离散或连续 Pitch Control。
- 读取最终计算音高。

#### 演唱版本

- 生成 Retake。
- 切换 Retake。
- 删除 Retake。

#### 工作流

- 根据当前编辑器和选区定位目标。
- 建立撤销点。
- 写后读回。
- 播放验证。

### 5.2 仍需调用方承担的工作

- 日语歌词分句与语义重音。
- 音乐风格规划。
- 批量音符映射。
- 处理等待和重试。
- 空集合与稀疏数据归一化。
- 0-based 与 1-based 索引判断。
- 写入是否真正生效的后置验证。
- 中途失败后的清理。

## 6. 优化建议

### P0：直接影响可靠性与可用性

#### 6.1 增加批量事务工具

建议提供 `sv_batch`，支持：

- 一次提交多个调用。
- 使用临时变量引用前一步返回的句柄。
- 自动创建一个撤销记录。
- 任一步失败时停止。
- 可选自动回滚。
- 返回每一步的结构化结果。
- 支持最终状态断言。

这会直接解决大量音符编辑缓慢和半完成状态问题。

#### 6.2 统一集合返回形状

以下情况必须统一：

- 空音符数组当前可能返回 `{}`。
- 空控制点数组当前可能返回 `{}`。
- 稀疏计算音高当前返回数字键对象。

建议所有集合始终返回数组，空集合固定为 `[]`；稀疏帧应显式包含帧索引或 `null`。

#### 6.3 统一索引规则

建议 MCP 对外统一使用 0-based 或 1-based，不让调用方根据对象类型猜测。

至少应在返回值中提供：

```json
{
  "index": 1,
  "indexBase": 0
}
```

Retake 的 0-based 与音符组常见的 1-based 是当前最危险的组合之一。

#### 6.4 提供结构化变更结果

所有修改操作建议返回：

```json
{
  "ok": true,
  "changed": true,
  "warnings": [],
  "postcondition": {}
}
```

当前已观察到：

- 覆盖已有速度标记没有生效但不报错。
- 删除不存在的 Retake 不报错。
- `unselectNote` 返回 `false` 但实际生效。

单纯的 `null`、`false` 或无错误不足以判断结果。

#### 6.5 提供处理等待工具

建议增加 `sv_wait_for_processing`：

- 等待音素非空。
- 等待计算属性就绪。
- 等待计算音高达到指定覆盖率。
- 支持超时、轮询间隔和进度。

#### 6.6 提供 singer 感知的轨道创建

建议增加：

```json
{
  "tool": "sv_create_track",
  "inheritVoiceFromTrack": 1
}
```

并返回明确状态：

```json
{
  "voiceAssigned": true,
  "renderReady": true
}
```

当前直接创建轨道可能得到等长空音素，克隆已有轨道才可靠。

### P1：显著提高音乐工作效率

#### 6.7 增加工程与选区快照

建议提供：

- `sv_inspect_project`
- `sv_inspect_selection`
- `sv_inspect_group`

一次返回轨道、音符组、歌词、声线、参数和选区摘要，避免数百次遍历。

#### 6.8 增加批量音乐操作

建议提供：

- `sv_replace_notes`
- `sv_patch_notes`
- `sv_set_lyrics`
- `sv_set_phonemes`
- `sv_apply_automation`
- `sv_apply_pitch_controls`
- `sv_generate_retakes`

这些工具不必替代通用调用，但应覆盖最常见的调音和填词流程。

#### 6.9 为计算结果提供自描述元数据

计算音高应同时返回：

- 起始 blick。
- 采样间隔。
- 请求帧数。
- 有声帧数。
- 缺失帧语义。
- 数值单位。

计算属性应明确标注字段是否适用于当前语言或演唱类型。

#### 6.10 封装 Pitch Control 坐标系统

连续曲线当前同时出现：

- 相对控制点位置。
- 绝对查询位置。

建议 MCP 接受显式坐标类型：

```json
{
  "position": 0.125,
  "unit": "quarter",
  "relativeTo": "curve"
}
```

#### 6.11 封装 Retake 工作流

建议提供高层工具处理：

- 生成维度。
- 处理等待。
- 活动 Take。
- 索引归一化。
- 测试 Take 清理。

#### 6.12 自动管理句柄

建议支持：

- 请求作用域句柄。
- 批量释放。
- 自动释放临时对象。
- 稳定对象 ID。

### P2：提升智能编辑体验

#### 6.13 增加预览与差异

建议提供 dry-run：

- 预计修改多少音符。
- 修改前后歌词、音高、时值和参数差异。
- 是否会新增轨道或音符组。
- 是否存在声库或处理风险。

#### 6.14 增加事件订阅

支持 MCP 订阅：

- 选区变化。
- 当前轨道变化。
- 工程变化。
- 处理完成。
- 播放状态变化。

这样可以从轮询式助手发展为真正跟随用户操作的 Copilot。

#### 6.15 保证 MCP 自描述完整

独立安装 MCP 后，不应要求使用者读取源码或外部文档才能安全调用。

建议提供：

- 运行时能力列表。
- 每个方法的参数与索引规则。
- 宿主版本支持状态。
- 返回结构示例。
- 已知宿主边界。

## 7. 下一阶段建议

从黑盒体验看，继续增加底层 API 覆盖的边际收益已经低于工作流封装。下一阶段最值得优先实现的是：

1. `sv_batch` 与回滚。
2. 集合和计算数据归一化。
3. 结构化修改结果与后置断言。
4. `sv_wait_for_processing`。
5. 工程与选区快照。
6. singer 感知的轨道创建。

完成这六项后，`sv-copilot` 才会从“完整但底层的远程 API”真正转变为适合自动调音、填词和编曲辅助的 MCP。
