# SV Copilot MCP 黑盒体验记录（专项测试前）

## 1. 记录目的

本文只从 MCP 使用者视角记录 `sv-copilot` 的实际体验。

- 不依赖仓库代码。
- 不依赖项目内部文档。
- 不假定桥接层如何实现。
- 只把 MCP 工具的输入、输出和 Synthesizer V 中可观察到的结果作为证据。

## 2. 已实际完成的工作

截至专项测试前，已通过 MCP 完成以下操作：

1. 检查桥接存活状态。
2. 获取工程、时间轴、编辑器、编排视图和播放控制等根对象。
3. 遍历工程中的音轨、音符组和音符。
4. 创建、删除、克隆音轨和音符。
5. 设置音符的音高、位置、时值、歌词和语言。
6. 设置声线的张力、气声、性别和音色偏移。
7. 写入响度、张力、气声、颤音和音高偏差自动化。
8. 修改速度标记。
9. 设置音轨增益与声像。
10. 读取日语音素并据此判断处理是否完成。
11. 控制播放、停止、定位，并读取播放状态。
12. 建立撤销记录。

当前工程中还通过 MCP 构建了两条可播放轨道：

- 主唱轨：69 个日语音符。
- 怒声叠唱轨：10 个日语音符。

两轨歌词、非空音素数量和播放状态均已从 MCP 读回验证。

## 3. 当前最明显的优点

### 3.1 通用对象调用覆盖面强

只要能够取得对象句柄，就可以继续遍历并调用对象方法。它不仅能读取工程，还能真实修改 Synthesizer V 当前工程。

### 3.2 读写闭环可成立

音符、歌词、自动化、速度和混音参数都能在写入后重新读回。对于自动化调声任务，这比只生成脚本文件更可靠。

### 3.3 可以执行较复杂的跨对象工作流

已经完成过以下组合操作：

- 创建撤销点。
- 重写整段旋律。
- 添加第二轨。
- 克隆已有声库绑定。
- 清空克隆内容。
- 重建怒声叠唱。
- 调整混音。
- 等待音素生成。
- 播放并验证。

这证明 MCP 不只是单次属性编辑器，而是可以组织完整工程操作。

## 4. 已遇到的黑盒问题

### 4.1 批量编辑成本很高

重写 69 个音符需要大量单独调用。每个音符通常至少涉及位置、时值、音高和属性等多个请求。调用耗时明显，并且任何中途失败都可能留下部分结果。

### 4.2 缺少一次性工程摘要

英文测试内容曾残留在同一音轨的第二个音符组中。为了定位它，必须逐层遍历音轨、音符组和歌词。使用者缺少一个能够直接返回工程结构摘要的工具。

### 4.3 新建音轨没有可靠继承 singer

直接创建的新轨看似具备声线参数，但音素结果是等长的空字符串数组。等待后仍然为空。后来改为克隆主唱轨并清空内容，音素才正常生成。

从使用者角度看，声线参数存在并不等于 singer 已经可以渲染。

### 4.4 处理完成状态不明确

音素接口可能返回长度正确但内容为空的数组。仅检查数组长度会误判为处理成功，必须额外检查每一项是否非空并进行等待重试。

### 4.5 写入返回成功不保证状态发生变化

尝试覆盖已有速度标记时，调用没有返回错误，但读回仍是旧速度。实际可行的步骤是先删除标记，再重新添加。

### 4.6 通用调用过于底层

使用者必须自行处理：

- 对象句柄。
- Lua 风格索引。
- 对象间引用。
- 撤销边界。
- 处理等待。
- 写后验证。
- 部分失败后的清理。

覆盖面很强，但工作流负担也很高。

### 4.7 缺少音乐语义层

MCP 可以准确执行“第几个音符改成什么”，但不会自动理解歌词语义、日语分句、重音位置、旋律叙事或怒音出现的上下文。这些仍需调用方规划后再翻译成大量底层请求。

## 5. 尚未进行的高价值黑盒测试

### 5.1 精细音素编辑

目标：确认能否直接读取和修改音素、音素位置、强度、活动度或发音覆盖，并在处理结果中观察到变化。

验收标准：

- 能读取目标音符的原始发音信息。
- 能写入一项可逆的发音变化。
- 能从音符或计算结果中读回变化。
- 能恢复原始状态。

### 5.2 计算音高与详细计算属性

目标：确认能否读取真正渲染后的音高曲线和详细音素属性，而不只是读取输入音符或自动化点。

验收标准：

- 返回非空计算结果。
- 结果能与指定音符组对应。
- 能区分输入参数和最终计算数据。

### 5.3 Pitch Control

目标：确认原生 Pitch Control Point 或 Pitch Control Curve 能否创建、添加、读取、修改和删除。

验收标准：

- 创建控制对象。
- 添加到目标音符组。
- 读取数量和内容。
- 修改后可读回。
- 删除后数量恢复。

### 5.4 Retake

目标：确认能否生成 Take、读取 Take 数量、切换当前 Take 并删除测试 Take。

验收标准：

- 能取得目标音符的 Retake 对象。
- 能生成至少一个测试 Take。
- 能观察 Take 数量变化。
- 能切换并清理测试 Take。

### 5.5 选区驱动编辑

目标：确认 MCP 能否读取 Synthesizer V 当前编辑器中的用户选区，并只操作所选音符或音符组。

验收标准：

- 能读取当前轨道和当前音符组。
- 能读取选中的音符或音符组。
- 在存在选区时能精确定位对象。
- 在无选区时返回清晰、可判断的状态，而不是模糊失败。

## 6. 专项测试原则

1. 全程只调用 `sv-copilot` MCP。
2. 不读取仓库代码或现有项目文档。
3. 每项测试先记录原状态。
4. 可能修改工程的测试必须创建撤销点。
5. 测试后尽量恢复原状态。
6. 不以“调用没有报错”作为成功证据。
7. 必须读回状态或计算结果才能判定通过。
8. 对官方宿主未暴露或无法表达的能力，明确记为边界，不虚构成功。
