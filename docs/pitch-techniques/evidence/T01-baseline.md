# T01 Baseline And Synthetic Corpus

Captured: `2026-08-03T13:37:30-03:00`

## Frozen Baseline

| Field | Evidence |
|---|---|
| Starting commit | `0c91ebfd6a69842fb1230b318194e6b828116ac9` |
| Server interface | `0.9.0` |
| Node | `v24.16.0` on `win32-x64` |
| Lua PIPE protocol | `2` |
| Routed operations / public tools | `41 / 8` |
| Current committed host-profile fixture | `synthv-2.2.1-win32-readonly-v1` |
| Host-profile content hash | `sha256_831dc7b40ed7213e8647b76e12dd7afd571d1d097326233452ed76e3c1e89b31` |
| Host-profile evidence hash | `sha256:62f3b298f3059d67212c2b60ebd2dadeba75f93de7e23f970c303b8071c7b3aa` |

The profile fixture identifies Synthesizer V Studio 2 Pro `2.2.1` on Windows. It is prior
read-only evidence, not a claim about this capture. `npm run doctor` reported the bridge as
`not_started`; no live host call, setter, or Undo record was made for T01.

The loaded and staging bridges both declare protocol `2` and `read_note_fingerprints_v1`.
Their source hashes differ only in text according to doctor. `dispatcher_test.lua` passed
`42/42` checks against each copy.

## Synthetic Corpus

`test/fixtures/pitch-techniques/synthetic-corpus.v1.json` is generated by
`tools/generate-pitch-technique-corpus.mjs` from the concentrated reference oracle. Its
manifested canonical hash is:

```text
sha256_8a214e57d33bac9ed6e94235880ca857258a2944b92990160387efdeb70bc484
```

The corpus contains 20 deterministic cases with explicit input, dense truth, mask,
invariants, tolerance, and seed. It covers endpoint-normalized up/down Richards, asymmetric
and degenerate cases, all relevant transient regimes, 4/5.5/7/9 Hz vibrato, tempo-step
uniform-seconds frequency, null/low-coverage correction evidence, three-way overlap, coarse
transition resolution, and a large finite phase case. It contains no project content or lyrics.

The loader rejects changed hashes, malformed manifests, unknown fields, missing case fields,
misaligned masks, and non-finite observed truth. The fixture can be regenerated with:

```powershell
node tools/generate-pitch-technique-corpus.mjs --write
```

## Offline Measurements

The following uses `tools/capture-pitch-technique-baseline.mjs`: 25 post-warmup in-memory
planner runs per synthetic scenario. It is a reproducible planner/serialization baseline, not
live-host performance evidence.

| Notes | Input bytes | Response bytes | Operations | Median | P95 | Host calls / Undo |
|---:|---:|---:|---:|---:|---:|---:|
| 12 | 745 | 7,090 | 6 | 1.504 ms | 2.486 ms | 0 / 0 |
| 373 | 752 | 7,149 | 6 | 1.881 ms | 2.588 ms | 0 / 0 |

No tokenizer dependency is bundled, so this baseline records deterministic bytes rather than
token counts. The existing static fixture sizes are 2,147 bytes for 10 notes and 78,546 bytes
for 373 notes. The actual served tools/list is 4,957 bytes for eight facade tools; exposing all
41 handlers would be 111,234 bytes. The worst two-operation `sv_describe` sample was 9,493
bytes and deferred one operation within the 16 KiB budget.

## Verification

| Command | Result |
|---|---|
| `node --test test/pitch-technique-corpus.test.mjs` | 3 passed |
| `npm test` | 803 passed in 51.376 s |
| `npm run smoke:mcp` | passed against the PIPE harness |
| `lua test/dispatcher_test.lua <loaded bridge>` | 42 passed |
| `lua test/dispatcher_test.lua staging/StartSynthVCopilotPipe.lua` | 42 passed |
| `node --test docs/pitch-techniques/reference/model.test.mjs docs/pitch-techniques/reference/contract.test.mjs` | 83 passed in 0.907 s |
| `git diff --exit-code -- api-docs` | clean |

The smoke and dispatcher checks use test harnesses. They establish protocol behavior only; H1
and all write-based host facts remain for their gated tasks.
